tablestore>=6.1,<7.0
pydantic>=2.9,<3.0
# Tablestore imports this native CRC implementation at runtime. Pinning it here
# ensures the FC deployment package includes the module rather than relying on
# an undeclared transitive dependency.
crc32c>=2.7,<3.0
