# ADR 005 Single region

Decision: deploy initially in Alibaba Cloud `cn-hangzhou`. Rejected alternative: multi-region active-active. A single region fits time and credit constraints; the trade-off is that a regional failure remains a documented limitation.
